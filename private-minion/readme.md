Example configuration for adding whois support to private minion.

Run this private minion using the helper script, this will mount the modules into the container. Specify your minion key to the script.
